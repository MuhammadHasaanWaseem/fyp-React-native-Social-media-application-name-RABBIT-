# Supabase Edge Functions

Account delete ab **Edge Function ke bina** chalta hai: `supabase-migrations/delete-own-account-rpc.sql` ko SQL Editor mein run karo, phir app `delete_own_account` RPC use karti hai.

Purana `user-self-deletion` function hata diya gaya hai.
